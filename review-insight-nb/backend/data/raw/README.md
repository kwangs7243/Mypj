# Raw Data

Raw external datasets will live here.

Do not commit large raw datasets until source, license, and size are confirmed.

Raw files may include extra columns, but experiment scripts should convert them to the stable project schema:

```text
text,label
```

