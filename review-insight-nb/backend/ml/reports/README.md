# Reports

Experiment result tables and generated evaluation summaries will live here.

Planned files:

- `experiment_results.csv`
- model comparison summaries
- tokenizer comparison summaries

Current first model comparison report:

```text
model_comparison_5k.csv
```

This report is generated by:

```bash
uv run python ml/experiments/compare_models.py
```

Report CSV files are ignored by Git because they are reproducible experiment outputs.
